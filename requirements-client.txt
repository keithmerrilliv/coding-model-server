# Client dependencies are declared in pyproject.toml under the `client` extra.
#
# Install the chat client with its optional niceties (rich, beautifulsoup4):
#     pip install -e '.[client]'
#
# This file is kept only as a pointer for `pip install -r` muscle memory; it
# intentionally has no pinned packages so it can't drift from pyproject.
-e .[client]
