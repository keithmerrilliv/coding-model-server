# Qwen Multi-Agent Server

# Model inference
llama-cpp-python>=0.3.20
transformers>=5.1.0
huggingface-hub>=1.4.0

# Web server (FastAPI)
fastapi>=0.128.1
uvicorn[standard]>=0.40.0
requests>=2.32.3
python-dotenv>=1.0.1

# RAG / memory
chromadb==1.4.1
sentence-transformers==5.2.2
pypdf>=5.1.0

# Code chunking (tree-sitter)
tree_sitter_languages

# Web search
ddgs>=9.10.0

# Autonomous mode — Jira sync (optional)
# The orchestrator daemon falls back to FakeJiraClient if this isn't installed
# or if JIRA_URL/EMAIL/TOKEN are unset, so it's a soft dependency.
atlassian-python-api>=4.0.0